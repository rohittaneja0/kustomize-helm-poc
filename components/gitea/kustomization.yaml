apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
- name: gitea
  version: 11.0.0
  repo: https://dl.gitea.com/charts/
  valuesFile: values.yaml

resources:
- test.yaml

namespace: gitea

enableHelm: true
