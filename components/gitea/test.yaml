apiVersion: v1
kind: Service
metadata:
  name: gitea
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80         # Service port (stable, cluster-internal)
      targetPort: 8080 # Pod port to forward to
  type: ClusterIP     # Default; exposes internally
